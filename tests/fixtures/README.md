# Test Fixtures

The test suite primarily generates `.docx` fixtures on the fly under `tmp_path` to keep tests deterministic and editable.

This directory is reserved for long-lived golden fixtures if needed later.
